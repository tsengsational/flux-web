{
    "name": "@flux-theatre/shared",
    "version": "0.1.0",
    "private": true,
    "description": "Shared TypeScript types and configurations for the Flux Theatre Ensemble monorepo",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "build": "tsc",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "typescript": "^5.7.0",
        "rimraf": "^6.0.0"
    }
}